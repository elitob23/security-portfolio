# Shared Python dependencies for the scripts in this folder.
#
# Dependencies are added here as scripts that need them are committed,
# with a pinned version and a comment naming the script that uses it.
#
# Scripts also carry their own requirements.txt in their subfolder, so a
# single script can be installed without the rest.
#
# Install with:
#   pip install -r requirements.txt

requests==2.34.2    # ip-auto-scan/ipautoscan.py
